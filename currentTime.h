#include <string>
#include <time.h>

// Get current date/time, format is YYYY-MM-DD.HH:mm:ss
const std::string currentDateTime();
std::string formatTimeDbStyle(tm );

std::string formatTimeFromString(std::string);

